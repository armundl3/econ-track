[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "econ-track"
version = "0.1.0"
description = "Static ETF trend and DCA allocation dashboard data engine"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = []

[tool.setuptools]
package-dir = {"" = "backend"}

[tool.setuptools.packages.find]
where = ["backend"]
